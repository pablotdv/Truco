@using Truco.ViewModels
@model CompeticoesEquipesViewModel
@using (Ajax.BeginForm("Pesquisa", "CompeticoesEquipes", new AjaxOptions()
{
    HttpMethod = "GET",
    InsertionMode = InsertionMode.Replace,
    UpdateTargetId = "pesquisa"
}))
{
	<div class="row form-group">
		<div class="col-md-4 col-xs-6">
			@Html.LabelFor(model => model.Equipe, htmlAttributes: new { @class = "control-label" })
			@Html.EditorFor(model => model.Equipe, new { htmlAttributes = new { @class = "form-control" } })
			@Html.ValidationMessageFor(model => model.Equipe, "", new { @class = "text-danger" })
		</div> 
	</div>
	<div class="row">
        <div class="col-lg-12">
            <div class="btn-group">                        
				<button type="submit" class="btn btn-default" id="submit" name="submit">Pesquisar</button>
            </div>
        </div>
    </div>
}

