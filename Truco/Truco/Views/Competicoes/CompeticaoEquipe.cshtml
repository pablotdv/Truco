@model Truco.Models.CompeticaoEquipe
@{
    ViewBag.Title = "Competição Equipe";
}

<hgroup>
    <h2>@ViewBag.Title.</h2>
    <h3>@ViewBag.Message</h3>
</hgroup>
<div class="row">
    <div class="col-md-12">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(a => a.CompeticaoId)            

            <div class="panel panel-default">
                <div class="panel-heading">
                    <label>Equipes</label>
                </div>
                <div class="panel-body">

                    @*para incluir especialidades do parceiro*@
                    @*@Html.Partial("_CadEquipe", new Truco.ViewModels.CompeticaoEquipeViewModel { CompeticaoId  = Model.CompeticaoId , Equipe = Model.CompeticaoEquipe})*@

                    @*para listar as especialidades do parceiro*@
                    <div id="equipesDiv">
                   @*     @Html.Partial("_ListaEquipe", Model.CompeticaoEquipeList)*@
                    </div>
                </div>
            </div>

            @*<div class="form-group">
                <div class="col-md-4 col-xs-6">
                    @Html.LabelFor(model => model.SorteioModo, htmlAttributes: new { @class = "control-label" })
                    @Html.EnumDropDownListFor(model => model.SorteioModo, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.SorteioModo, "", new { @class = "text-danger" })
                </div>
            </div>*@
            //Pesquisar equipes competição


            <div class="form-group">
                <div class="col-lg-12">
                    <a href="@Url.Action("Indice")" class="btn btn-default"><i class="fa fa-arrow-left fa-fw"></i> Voltar</a>
                    @*<button type="submit" class="btn btn-primary"><i class="fa fa-save fa-fw"></i> sadasdas</button>*@
                </div>
            </div>
        }


    </div>
</div>
